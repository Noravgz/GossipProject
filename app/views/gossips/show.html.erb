<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">GOSSIP PROJECT</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to 'Gossips', gossips_path, class: 'nav-link active', aria: { current: 'page' } %>
        <%= link_to 'Contacts', contact_path, class: 'nav-link' %>
        <%= link_to 'Notre équipe', team_path, class: 'nav-link' %> 
      </div>
    </div>
  </div>
</nav>


<h1 class="text-center">Voici un chouette potin écris par :</h1>


<div class="d-flex justify-content-center align-items-center" style="height: 70vh;">
    <div class="card" style="width: 30rem; border-radius: 15px;">
        <div class="card-body">
        <h1 class="text-center"><%= link_to @gossip.user.first_name, user_path(@gossip.user) %></h1>
        <h3 class="text-center">Habite dans la ville : <%= @gossip.user.city.name %></h3>
        <h3 class="text-center"><%= @gossip.title %></h3>
        <p class="text-center"><%= @gossip.content %></p>
        <p class="text-center">
        <%= link_to 'Retour à la liste des potins', gossips_path, class: 'btn btn-primary', role: 'button' %>
        </p>
        <p class="text-center">
        <%= link_to "Voir tout les potins de #{@gossip.user.city.name}", city_path(@gossip.user.city), class: 'btn btn-primary', role: 'button' %>
        </p>
        </div>
    </div>
</div>
